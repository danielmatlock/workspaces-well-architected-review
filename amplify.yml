version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - echo "Static site — no build step required"
  artifacts:
    baseDirectory: src
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
